import { StyleSheet } from "react-native"

export default StyleSheet.create({
    flexCol: {
        flexDirection: "column",
    },
    flexRow: {
        flexDirection: "row",
    },
    flex: {
        flex: 1,
    },
})
